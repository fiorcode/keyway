import 'package:flutter/material.dart';

class ErrorBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset("assets/ic_error.png"),
    );
  }
}
